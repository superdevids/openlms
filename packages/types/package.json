{
  "name": "@openlms/types",
  "version": "0.1.0",
  "private": true,
  "description": "openlms shared types & enums — satu sumber kontrak (dipakai apps/api, apps/web, packages/database)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "echo \"@openlms/types: no tests in F0\"",
    "test:unit": "echo \"@openlms/types: no tests in F0\""
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
