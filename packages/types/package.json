{
  "name": "@opensis/types",
  "version": "0.1.0",
  "private": true,
  "description": "opensis shared types & enums — satu sumber kontrak (dipakai apps/api, apps/web, packages/database)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "echo \"@opensis/types: no tests in F0\"",
    "test:unit": "echo \"@opensis/types: no tests in F0\""
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
