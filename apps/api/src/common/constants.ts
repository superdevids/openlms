/** Konstanta global aplikasi API (F0-T8: security-related defaults). */

export const GLOBAL_PREFIX = "api/v1";

/** Nama header request ID (docs/02 §11, docs/04 §1.6). */
export const REQUEST_ID_HEADER = "x-request-id";

/** Prefix request ID internal. */
export const REQUEST_ID_PREFIX = "req_";
